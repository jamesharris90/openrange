{
  "name": "openrange",
  "private": true,
  "engines": {
    "node": "22.x"
  },
  "scripts": {
    "build": "cd server && npm ci && cd ../client && npm install && npm run build",
    "start": "cd server && node index.js"
  },
  "dependencies": {
    "multer": "^2.0.2",
    "node-cron": "^4.2.1",
    "pdf-parse": "^2.4.5",
    "xlsx": "^0.18.5"
  },
  "devDependencies": {
    "nodemon": "^3.1.11"
  }
}
