# Deprecated Pages

The following pages are deprecated as part of the Trading Session Command Center migration:

- ExpectedMovePage → replaced by Post-Market Review and command-center workflows.
- AIQuantPage → replaced by Open Market Radar command workflows.
- OldOpenMarketPage (`/open-market`) → redirected to `/open-market-radar`.
- OldPreMarketPage (`/pre-market`) → redirected to `/pre-market-command`.
